<h1>Listing scenes</h1>

<table>
  <thead>
    <tr>
      <th>Review</th>
      <th>Sort order</th>
      <th>Max length</th>
      <th>Name</th>
      <th>Description</th>
      <th>Scene template</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @scenes.each do |scene| %>
      <tr>
        <td><%= scene.review %></td>
        <td><%= scene.sort_order %></td>
        <td><%= scene.max_length %></td>
        <td><%= scene.name %></td>
        <td><%= scene.description %></td>
        <td><%= scene.scene_template %></td>
        <td><%= link_to 'Show', scene %></td>
        <td><%= link_to 'Edit', edit_scene_path(scene) %></td>
        <td><%= link_to 'Destroy', scene, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Scene', new_scene_path %>
