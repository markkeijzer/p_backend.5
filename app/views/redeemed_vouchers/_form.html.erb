<%= form_for(@redeemed_voucher) do |f| %>
  <% if @redeemed_voucher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@redeemed_voucher.errors.count, "error") %> prohibited this redeemed_voucher from being saved:</h2>

      <ul>
      <% @redeemed_voucher.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :voucher_id %><br>
    <%= f.text_field :voucher_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
